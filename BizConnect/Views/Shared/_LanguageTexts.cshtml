@{
    var isThaiLanguage = ViewBag.IsThaiLanguage ?? false;
}

@*
This partial view contains language-specific text resources for the UI.
Usage: @await Html.PartialAsync("_LanguageTexts")
Then reference texts using the stored variables.
*@

@{
    // Common UI Text
    ViewBag.TextHome = isThaiLanguage ? "หน้าหลัก" : "Home";
    ViewBag.TextLogin = isThaiLanguage ? "เข้าสู่ระบบ" : "Login";
    ViewBag.TextLogout = isThaiLanguage ? "ออกจากระบบ" : "Logout";
    ViewBag.TextRegister = isThaiLanguage ? "ลงทะเบียน" : "Register";
    ViewBag.TextDashboard = isThaiLanguage ? "แดชบอร์ด" : "Dashboard";
    ViewBag.TextAdmin = isThaiLanguage ? "ผู้ดูแลระบบ" : "Admin";
    ViewBag.TextSettings = isThaiLanguage ? "การตั้งค่า" : "Settings";
    ViewBag.TextProfile = isThaiLanguage ? "โปรไฟล์" : "Profile";
    ViewBag.TextPrivacy = isThaiLanguage ? "นโยบายความเป็นส่วนตัว" : "Privacy";
    
    // OTAC Related Text
    ViewBag.TextOtacCode = isThaiLanguage ? "รหัส OTAC" : "OTAC Code";
    ViewBag.TextOtacGenerate = isThaiLanguage ? "สร้างรหัส OTAC" : "Generate OTAC";
    ViewBag.TextOtacValidate = isThaiLanguage ? "ตรวจสอบรหัส OTAC" : "Validate OTAC";
    ViewBag.TextOtacHistory = isThaiLanguage ? "ประวัติรหัส OTAC" : "OTAC History";
    ViewBag.TextOtacExpired = isThaiLanguage ? "รหัสหมดอายุ" : "Expired Codes";
    ViewBag.TextOtacActive = isThaiLanguage ? "รหัสที่ใช้งานได้" : "Active Codes";
    ViewBag.TextOtacUsed = isThaiLanguage ? "รหัสที่ใช้แล้ว" : "Used Codes";
    
    // Registration Related Text
    ViewBag.TextRegistrations = isThaiLanguage ? "การลงทะเบียน" : "Registrations";
    ViewBag.TextPending = isThaiLanguage ? "รอดำเนินการ" : "Pending";
    ViewBag.TextCompleted = isThaiLanguage ? "เสร็จสิ้น" : "Completed";
    ViewBag.TextFailed = isThaiLanguage ? "ล้มเหลว" : "Failed";
    ViewBag.TextSuccess = isThaiLanguage ? "สำเร็จ" : "Success";
    
    // Form Fields
    ViewBag.TextFullName = isThaiLanguage ? "ชื่อ-นามสกุล" : "Full Name";
    ViewBag.TextIdNumber = isThaiLanguage ? "เลขบัตรประจำตัวประชาชน" : "ID Number";
    ViewBag.TextMobileNumber = isThaiLanguage ? "เบอร์มือถือ" : "Mobile Number";
    ViewBag.TextAccountNumber = isThaiLanguage ? "เลขที่บัญชี" : "Account Number";
    ViewBag.TextBranch = isThaiLanguage ? "สาขา" : "Branch";
    ViewBag.TextPurpose = isThaiLanguage ? "วัตถุประสงค์" : "Purpose";
    ViewBag.TextIssuedTo = isThaiLanguage ? "ออกให้กับ" : "Issued To";
    
    // Actions
    ViewBag.TextSubmit = isThaiLanguage ? "ส่งข้อมูล" : "Submit";
    ViewBag.TextCancel = isThaiLanguage ? "ยกเลิก" : "Cancel";
    ViewBag.TextEdit = isThaiLanguage ? "แก้ไข" : "Edit";
    ViewBag.TextDelete = isThaiLanguage ? "ลบ" : "Delete";
    ViewBag.TextView = isThaiLanguage ? "ดู" : "View";
    ViewBag.TextSave = isThaiLanguage ? "บันทึก" : "Save";
    ViewBag.TextClose = isThaiLanguage ? "ปิด" : "Close";
    ViewBag.TextConfirm = isThaiLanguage ? "ยืนยัน" : "Confirm";
    
    // Status Messages
    ViewBag.TextLoading = isThaiLanguage ? "กำลังโหลด..." : "Loading...";
    ViewBag.TextNoData = isThaiLanguage ? "ไม่มีข้อมูล" : "No data";
    ViewBag.TextError = isThaiLanguage ? "เกิดข้อผิดพลาด" : "Error";
    ViewBag.TextSuccessMessage = isThaiLanguage ? "ดำเนินการสำเร็จ" : "Operation successful";
    
    // User Management
    ViewBag.TextUsers = isThaiLanguage ? "ผู้ใช้งาน" : "Users";
    ViewBag.TextAddUser = isThaiLanguage ? "เพิ่มผู้ใช้งาน" : "Add User";
    ViewBag.TextUserManagement = isThaiLanguage ? "จัดการผู้ใช้งาน" : "User Management";
    ViewBag.TextRoles = isThaiLanguage ? "บทบาท" : "Roles";
    ViewBag.TextPermissions = isThaiLanguage ? "สิทธิ์การใช้งาน" : "Permissions";
    
    // System
    ViewBag.TextSystem = isThaiLanguage ? "ระบบ" : "System";
    ViewBag.TextLogs = isThaiLanguage ? "บันทึกเหตุการณ์" : "Logs";
    ViewBag.TextBackup = isThaiLanguage ? "สำรองข้อมูล" : "Backup";
    ViewBag.TextImport = isThaiLanguage ? "นำเข้าข้อมูล" : "Import Data";
    ViewBag.TextExport = isThaiLanguage ? "ส่งออกข้อมูล" : "Export Data";
    
    // Date and Time
    ViewBag.TextToday = isThaiLanguage ? "วันนี้" : "Today";
    ViewBag.TextThisMonth = isThaiLanguage ? "เดือนนี้" : "This Month";
    ViewBag.TextThisYear = isThaiLanguage ? "ปีนี้" : "This Year";
    ViewBag.TextCreatedAt = isThaiLanguage ? "สร้างเมื่อ" : "Created At";
    ViewBag.TextUpdatedAt = isThaiLanguage ? "อัปเดตเมื่อ" : "Updated At";
    ViewBag.TextExpiresAt = isThaiLanguage ? "หมดอายุเมื่อ" : "Expires At";
    
    // KBank ODD Specific
    ViewBag.TextOddRegistration = isThaiLanguage ? "การลงทะเบียน KBank ODD" : "KBank ODD Registration";
    ViewBag.TextOddManagement = isThaiLanguage ? "จัดการ ODD" : "ODD Management";
    ViewBag.TextKBankSystem = isThaiLanguage ? "ระบบ KBank ODD" : "KBank ODD System";
    
    // Validation Messages
    ViewBag.TextRequired = isThaiLanguage ? "กรุณากรอกข้อมูล" : "This field is required";
    ViewBag.TextInvalidFormat = isThaiLanguage ? "รูปแบบข้อมูลไม่ถูกต้อง" : "Invalid format";
    ViewBag.TextOtacInvalid = isThaiLanguage ? "รหัส OTAC ไม่ถูกต้อง" : "Invalid OTAC code";
    ViewBag.TextOtacExpiredMsg = isThaiLanguage ? "รหัส OTAC หมดอายุแล้ว" : "OTAC code has expired";
    
    // Statistics
    ViewBag.TextTotal = isThaiLanguage ? "ทั้งหมด" : "Total";
    ViewBag.TextActive = isThaiLanguage ? "ใช้งานได้" : "Active";
    ViewBag.TextExpired = isThaiLanguage ? "หมดอายุ" : "Expired";
    ViewBag.TextUsed = isThaiLanguage ? "ใช้แล้ว" : "Used";
    ViewBag.TextGenerated = isThaiLanguage ? "สร้างแล้ว" : "Generated";
    ViewBag.TextSuccessRate = isThaiLanguage ? "อัตราความสำเร็จ" : "Success Rate";
    ViewBag.TextUsageRate = isThaiLanguage ? "อัตราการใช้งาน" : "Usage Rate";
    
    // Registration Form Specific Text
    ViewBag.TextVerifyOtac = isThaiLanguage ? "ยืนยัน OTAC" : "Verify OTAC";
    ViewBag.TextFillInformation = isThaiLanguage ? "กรอกข้อมูล" : "Fill Information";
    ViewBag.TextPleaseFillInfo = isThaiLanguage ? "กรุณากรอกข้อมูลของท่านให้ครบถ้วนและถูกต้อง" : "Please fill in your information completely and accurately";
    ViewBag.TextVerifiedOtacCode = isThaiLanguage ? "รหัส OTAC ที่ยืนยันแล้ว:" : "Verified OTAC Code:";
    
    // Form Section Headers
    ViewBag.TextPersonalInfo = isThaiLanguage ? "ข้อมูลส่วนตัว" : "Personal Information";
    ViewBag.TextContactInfo = isThaiLanguage ? "ข้อมูลการติดต่อ" : "Contact Information";
    ViewBag.TextBankAccountInfo = isThaiLanguage ? "ข้อมูลบัญชีธนาคาร" : "Bank Account Information";
    
    // Form Field Labels
    ViewBag.TextFullNameLabel = isThaiLanguage ? "ชื่อ-นามสกุล" : "Full Name";
    ViewBag.TextNationalIdLabel = isThaiLanguage ? "เลขบัตรประชาชน (13 หลัก)" : "National ID Number (13 digits)";
    ViewBag.TextMobileLabel = isThaiLanguage ? "เบอร์มือถือ" : "Mobile Number";
    ViewBag.TextAccountLabel = isThaiLanguage ? "เลขที่บัญชี" : "Account Number";
    ViewBag.TextBranchLabel = isThaiLanguage ? "สาขาที่เปิดบัญชี" : "Branch where account was opened";
    ViewBag.TextSelectBranch = isThaiLanguage ? "เลือกสาขา" : "Select Branch";
    
    // Field Hints and Instructions
    ViewBag.TextFullNameHint = isThaiLanguage ? "กรุณากรอกชื่อ-นามสกุลตามเอกสารทางราชการ" : "Please enter full name as per official documents";
    ViewBag.TextNationalIdHint = isThaiLanguage ? "ใช้เฉพาะบัตรประชาชนไทยเท่านั้น - กรอกตัวเลข 13 หลัก" : "Thai National ID only - enter 13 digits";
    ViewBag.TextMobileHint = isThaiLanguage ? "รูปแบบ: 08XXXXXXXX หรือ 09XXXXXXXX (10 หลัก)" : "Format: 08XXXXXXXX or 09XXXXXXXX (10 digits)";
    ViewBag.TextAccountHint = isThaiLanguage ? "เลขบัญชี KBank 10-15 หลัก (ไม่ต้องใส่เครื่องหมายขีด)" : "KBank account number 10-15 digits (no dashes)";
    
    // Terms and Privacy
    ViewBag.TextIHaveReadAndAccept = isThaiLanguage ? "ข้าพเจ้าได้อ่านและยอมรับ" : "I have read and accept";
    ViewBag.TextTermsAndConditions = isThaiLanguage ? "ข้อกำหนดและเงื่อนไข" : "Terms and Conditions";
    ViewBag.TextPrivacyPolicy = isThaiLanguage ? "นโยบายความเป็นส่วนตัว" : "Privacy Policy";
    ViewBag.TextAccept = isThaiLanguage ? "ยอมรับ" : "Accept";
    ViewBag.TextMustAcceptTerms = isThaiLanguage ? "กรุณายอมรับข้อกำหนดและเงื่อนไขก่อนดำเนินการต่อ" : "Please accept the terms and conditions before proceeding";
    
    // Buttons
    ViewBag.TextRegisterDirectDebit = isThaiLanguage ? "ลงทะเบียนหักบัญชีอัตโนมัติ" : "Register for Direct Debit";
    
    // Security Section
    ViewBag.TextSecurityTitle = isThaiLanguage ? "การรักษาความปลอดภัย" : "Security";
    ViewBag.TextSecurityNotice = isThaiLanguage ? "ข้อมูลของท่านจะถูกเข้ารหัสและส่งต่อไปยังระบบ KBank อย่างปลอดภัย โดยเป็นไปตามมาตรฐานความปลอดภัยระดับธนาคาร" : "Your data will be encrypted and securely transmitted to KBank system in accordance with banking-level security standards";
    
    // Terms Modal Content
    ViewBag.TextTermsModalTitle = isThaiLanguage ? "ข้อกำหนดและเงื่อนไข" : "Terms and Conditions";
    ViewBag.TextTermsServiceUsage = isThaiLanguage ? "การใช้บริการ" : "Service Usage";
    ViewBag.TextTermsServiceUsageContent = isThaiLanguage ? "ท่านตกลงใช้บริการหักบัญชีอัตโนมัติตามเงื่อนไขที่กำหนดโดย KBank และ BizConnect" : "You agree to use the direct debit service according to the terms and conditions set by KBank and BizConnect";
    ViewBag.TextTermsDataAccuracy = isThaiLanguage ? "ความถูกต้องของข้อมูล" : "Data Accuracy";
    ViewBag.TextTermsDataAccuracyContent = isThaiLanguage ? "ท่านรับรองว่าข้อมูลที่ให้ไว้ถูกต้องและเป็นปัจจุบัน" : "You certify that the information provided is accurate and current";
    ViewBag.TextTermsResponsibility = isThaiLanguage ? "ความรับผิดชอบ" : "Responsibility";
    ViewBag.TextTermsResponsibilityContent = isThaiLanguage ? "ท่านรับผิดชอบต่อการใช้บริการและธุรกรรมที่เกิดขึ้น" : "You are responsible for service usage and transactions that occur";
    ViewBag.TextTermsModifications = isThaiLanguage ? "การแก้ไขข้อกำหนด" : "Terms Modifications";
    ViewBag.TextTermsModificationsContent = isThaiLanguage ? "บริษัทขอสงวนสิทธิ์ในการแก้ไขข้อกำหนดโดยแจ้งให้ทราบล่วงหน้า" : "The company reserves the right to modify terms with advance notice";
    
    // Privacy Modal Content
    ViewBag.TextPrivacyModalTitle = isThaiLanguage ? "นโยบายความเป็นส่วนตัว" : "Privacy Policy";
    ViewBag.TextPrivacyDataCollection = isThaiLanguage ? "การเก็บรวบรวมข้อมูล" : "Data Collection";
    ViewBag.TextPrivacyDataCollectionContent = isThaiLanguage ? "เราเก็บรวบรวมข้อมูลที่จำเป็นสำหรับการให้บริการเท่านั้น" : "We collect only necessary data for service provision";
    ViewBag.TextPrivacyDataUsage = isThaiLanguage ? "การใช้ข้อมูล" : "Data Usage";
    ViewBag.TextPrivacyDataUsageContent = isThaiLanguage ? "ข้อมูลจะใช้เพื่อการให้บริการและการปรับปรุงบริการ" : "Data will be used for service provision and service improvement";
    ViewBag.TextPrivacyDataDisclosure = isThaiLanguage ? "การเปิดเผยข้อมูล" : "Data Disclosure";
    ViewBag.TextPrivacyDataDisclosureContent = isThaiLanguage ? "เราจะไม่เปิดเผยข้อมูลให้บุคคลที่สาม ยกเว้นตามที่กฎหมายกำหนด" : "We will not disclose data to third parties except as required by law";
    ViewBag.TextPrivacyDataSecurity = isThaiLanguage ? "ความปลอดภัย" : "Security";
    ViewBag.TextPrivacyDataSecurityContent = isThaiLanguage ? "เรามีมาตรการรักษาความปลอดภัยข้อมูลตามมาตรฐานสากล" : "We have data security measures according to international standards";
    
    // JavaScript Validation Messages
    ViewBag.TextSubmittingData = isThaiLanguage ? "กำลังส่งข้อมูล..." : "Submitting data...";
    ViewBag.TextNationalIdValidation = isThaiLanguage ? "เลขประจำตัวประชาชนต้องเป็นตัวเลข 13 หลัก" : "National ID number must be 13 digits";
    ViewBag.TextNationalIdHintValidation = isThaiLanguage ? "กรอกเลขประจำตัวประชาชน 13 หลัก" : "Enter 13-digit National ID number";
    ViewBag.TextFullNameValidation = isThaiLanguage ? "ชื่อ-นามสกุลต้องมี 2-200 ตัวอักษร" : "Full name must be 2-200 characters";
    ViewBag.TextMobileValidation = isThaiLanguage ? "เบอร์มือถือต้องขึ้นต้นด้วย 08 หรือ 09 และมี 10 หลัก" : "Mobile number must start with 08 or 09 and have 10 digits";
    ViewBag.TextAccountValidation = isThaiLanguage ? "เลขที่บัญชีต้องเป็นตัวเลข 10-15 หลัก" : "Account number must be 10-15 digits";
    ViewBag.TextBranchValidation = isThaiLanguage ? "กรุณาเลือกสาขา" : "Please select a branch";
    ViewBag.TextCheckFormData = isThaiLanguage ? "กรุณาตรวจสอบข้อมูลที่กรอกให้ครบถ้วนและถูกต้อง" : "Please check that all information is complete and correct";
}